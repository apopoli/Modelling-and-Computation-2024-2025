function [phi]=FEM2D_s2(msh,p,t,BC)

% import mesh info

% cycle to compute and assemble element matrix and right-hand-side element array


% Boundary conditions
% nodal_BC = set_BCs_on_nodes(msh, BC); % BC values on nodes

% Solve linear system

end